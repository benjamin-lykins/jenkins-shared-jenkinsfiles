properties([
  parameters([
     stringParam(name: 'Name', defaultValue: ""),
     booleanParam(name: 'Boolean', defaultValue: true),
  ])
])


node {
    stage('Echo') {
        echo 'Hello World!'
        echo 'Jenkinsfile2'
    }
}
