properties([
  parameters([
     stringParam(name: 'Name', defaultValue: ""),
     boolParam(name: 'Name', defaultValue: true),
  ])
])


node {
    stage('Echo') {
        echo 'Hello World!'
        echo 'Jenkinsfile2'
    }
}
