properties([
  parameters([
     stringParam(name: 'Name', defaultValue: ""),
     booleanParam(name: 'Name', defaultValue: true),
  ])
])


node {
    stage('Echo') {
        echo 'Hello World!'
        echo 'Jenkinsfile2'
    }
}
